# Автоматическая сборка Android AAB - Готово к использованию! 🚀

## Текущий статус: ✅ ПОЛНОСТЬЮ НАСТРОЕНО

Ваш проект **AI Accountant** полностью готов для автоматической сборки Android App Bundle (AAB) через GitHub Actions.

## Что уже настроено

### ✅ GitHub Actions Workflow
**Файл:** `.github/workflows/android-build.yml`

**Включает:**
- Полную настройку Android SDK (API 34)
- Установку Java JDK 17 и Node.js 20
- Автоматическую сборку веб-приложения
- Синхронизацию с Capacitor
- Поддержку подписи приложений через keystore
- Сборку debug и release версий AAB
- Автоматическую загрузку artifacts

### ✅ Android Build Configuration
**Файл:** `android/app/build.gradle`

**Настроено:**
- Загрузка настроек из `key.properties`
- Конфигурация signing для release сборок
- Debug и release buildTypes

### ✅ Security Configuration
**Файлы:** `.gitignore`, `android/.gitignore`

**Защищено:**
- Keystore файлы не попадут в git
- key.properties игнорируется
- Секреты остаются в безопасности

### ✅ Git Repository
- Git инициализирован
- Ветка main создана
- Первый коммит выполнен
- Готов к push на GitHub

## Схема автоматической работы

```
┌─────────────────────────────────────────────────────────────────┐
│                    АВТОМАТИЧЕСКИЕ ТРИГГЕРЫ                      │
└─────────────────────────────────────────────────────────────────┘
                                │
        ┌───────────────────────┼───────────────────────┐
        │                       │                       │
        ▼                       ▼                       ▼
┌───────────────┐      ┌───────────────┐      ┌───────────────┐
│  Push в main  │      │ Pull Request  │      │Ручной запуск  │
│   или develop │      │               │      │ через Actions │
└───────┬───────┘      └───────┬───────┘      └───────┬───────┘
        │                      │                      │
        └──────────────────────┼──────────────────────┘
                               │
                               ▼
        ┌──────────────────────────────────────────┐
        │     GitHub Actions Workflow Start        │
        └──────────────────────────────────────────┘
                               │
        ┌──────────────────────┴──────────────────────┐
        │                                             │
        ▼                                             ▼
┌──────────────────┐                      ┌──────────────────┐
│ Setup Environment│                      │ Install Tools    │
│ - Ubuntu Latest  │                      │ - Node.js 20     │
│ - Checkout Code  │                      │ - Java JDK 17    │
└────────┬─────────┘                      │ - Android SDK 34 │
         │                                └────────┬─────────┘
         └────────────────┬───────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────────┐
        │         Build Web Application           │
        │   npm ci && npm run build               │
        └──────────────────┬──────────────────────┘
                           │
                           ▼
        ┌─────────────────────────────────────────┐
        │      Sync Capacitor Android             │
        │       npx cap sync android              │
        └──────────────────┬──────────────────────┘
                           │
                           ▼
        ┌─────────────────────────────────────────┐
        │      Decode Keystore from Secrets       │
        │  Create key.properties from Secrets     │
        └──────────────────┬──────────────────────┘
                           │
        ┌──────────────────┴──────────────────┐
        │                                     │
        ▼                                     ▼
┌──────────────────┐              ┌──────────────────┐
│  Debug Build     │              │  Release Build   │
│ bundleDebug      │              │ bundleRelease    │
└────────┬─────────┘              └────────┬─────────┘
         │                                 │
         └────────────────┬────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────────┐
        │        Upload AAB as Artifact           │
        │  - android-debug-aab (14 days)          │
        │  - android-release-aab (30 days)        │
        │  - mapping files (90 days)              │
        └──────────────────┬──────────────────────┘
                           │
                           ▼
        ┌─────────────────────────────────────────┐
        │          Build Summary Report           │
        │  - Build type, branch, commit           │
        │  - AAB file size                        │
        └─────────────────────────────────────────┘
                           │
                           ▼
                    ✅ ГОТОВО!
            Скачайте AAB из Artifacts
```

## Что нужно сделать для запуска

### 1. Создайте Keystore

```bash
keytool -genkey -v -keystore release.keystore \
  -alias ai-accountant \
  -keyalg RSA \
  -keysize 2048 \
  -validity 10000
```

### 2. Конвертируйте в Base64

```bash
cat release.keystore | base64 > release.keystore.base64
```

### 3. Создайте GitHub репозиторий

1. Создайте новый репозиторий на GitHub
2. Настройте 4 secrets (см. инструкцию ниже)

### 4. Загрузите код

```bash
git remote add origin https://github.com/ваш-пользователь/ai-accountant.git
git push -u origin main
```

**Сборка запустится автоматически!**

## GitHub Secrets (обязательно настроить)

Откройте: `Settings` → `Secrets and variables` → `Actions`

| Secret Name | Где взять значение |
|-------------|-------------------|
| `ANDROID_KEYSTORE_BASE64` | Содержимое `release.keystore.base64` |
| `ANDROID_KEYSTORE_PASSWORD` | Пароль, который вы ввели при создании keystore |
| `ANDROID_KEY_PASSWORD` | Обычно такой же, как пароль keystore |
| `ANDROID_KEY_ALIAS` | `ai-accountant` (указали при создании) |

## Как скачать готовый AAB

1. Перейдите в **Actions** на GitHub
2. Выберите последний успешный workflow (зеленая галочка ✅)
3. Прокрутите до секции **Artifacts**
4. Скачайте **android-release-aab**

## Файлы проекта

### Конфигурация
- `.github/workflows/android-build.yml` - GitHub Actions workflow
- `android/app/build.gradle` - Android build конфигурация
- `.gitignore`, `android/.gitignore` - Игнорирование секретов

### Документация
- **АВТОМАТИЧЕСКАЯ_СБОРКА.md** - Полная документация
- **БЫСТРЫЙ_СТАРТ_АВТОСБОРКИ.md** - Краткая инструкция за 5 минут
- **README_АВТОСБОРКА.md** (этот файл) - Обзор и статус

## Команды для работы

```bash
# Локальная сборка веб-приложения
npm run build

# Синхронизация с Android
npm run cap:sync

# Локальная сборка AAB (требует keystore)
cd android && ./gradlew bundleRelease

# Просмотр статуса GitHub Actions (через GitHub CLI)
gh workflow view "Build Android AAB"

# Ручной запуск сборки
gh workflow run android-build.yml -f build_type=release
```

## Следующие шаги

1. ✅ **Создайте keystore** (5 минут)
2. ✅ **Настройте GitHub Secrets** (2 минуты)
3. ✅ **Создайте GitHub репозиторий**
4. ✅ **Загрузите код** `git push -u origin main`
5. ✅ **Наблюдайте за автоматической сборкой** в Actions
6. ✅ **Скачайте AAB** из Artifacts
7. ✅ **Загрузите в Google Play Console**

## Полезные ссылки

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Android App Bundle](https://developer.android.com/guide/app-bundle)
- [Google Play Console](https://play.google.com/console)
- [Capacitor Android](https://capacitorjs.com/docs/android)

---

**Статус:** ✅ Готово к автоматической сборке
**Последнее обновление:** 2025-12-12

🚀 Теперь каждый push в main/develop будет автоматически собирать ваше приложение!
