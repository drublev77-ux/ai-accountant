# 🚀 Автоматический Deploy на Netlify

Этот проект настроен для автоматического деплоя на Netlify через GitHub Actions.

## 📋 Предварительные требования

1. **GitHub репозиторий** - ваш код должен быть в GitHub репозитории
2. **Netlify аккаунт** - зарегистрируйтесь на [app.netlify.com](https://app.netlify.com)

## ⚙️ Настройка автоматического деплоя

### Шаг 1: Создание сайта на Netlify

1. Войдите в [app.netlify.com](https://app.netlify.com)
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **GitHub** и авторизуйтесь
4. Выберите ваш репозиторий
5. Настройки билда (должны быть автоматически определены):
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
   - **Node version**: `20`
6. Нажмите **"Deploy site"**

### Шаг 2: Получение токенов Netlify

#### 2.1 Получить NETLIFY_AUTH_TOKEN

1. Перейдите в [app.netlify.com/user/applications](https://app.netlify.com/user/applications)
2. Нажмите **"New access token"**
3. Введите описание (например, "GitHub Actions Deploy")
4. Скопируйте созданный токен ⚠️ (он больше не будет показан)

#### 2.2 Получить NETLIFY_SITE_ID

1. Откройте ваш сайт на Netlify
2. Перейдите в **Site settings** → **General** → **Site details**
3. Найдите **Site ID** (например: `1234abcd-5678-90ef-ghij-klmnopqrstuv`)
4. Скопируйте этот ID

### Шаг 3: Добавление секретов в GitHub

1. Откройте ваш GitHub репозиторий
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Добавьте два секрета:

   **Секрет 1:**
   - **Name**: `NETLIFY_AUTH_TOKEN`
   - **Value**: токен из шага 2.1

   **Секрет 2:**
   - **Name**: `NETLIFY_SITE_ID`
   - **Value**: ID сайта из шага 2.2

### Шаг 4: Активация автоматического деплоя

После добавления секретов, автоматический деплой будет работать при:

- ✅ Push в ветку `main` или `master`
- ✅ Создании Pull Request в `main` или `master`

## 🔄 Как это работает

### Автоматический деплой при Push

1. Вы делаете `git push` в ветку `main/master`
2. GitHub Actions автоматически запускается
3. Устанавливаются зависимости (`npm ci`)
4. Выполняется сборка проекта (`npm run build`)
5. Готовый сайт деплоится на Netlify
6. Netlify возвращает URL для просмотра

### Preview деплой для Pull Request

1. Создаете Pull Request
2. GitHub Actions создает preview деплой
3. В комментарии PR появляется ссылка на preview
4. После мерджа PR, изменения попадают в production

## 📁 Конфигурационные файлы

### `.github/workflows/netlify-deploy.yml`
GitHub Actions workflow для автоматического деплоя.

### `netlify.toml`
Конфигурация Netlify с настройками:
- 🔄 Редиректы для SPA (Single Page Application)
- 🔒 Security headers (защита от XSS, clickjacking, etc.)
- ⚡ Кеширование статических файлов (1 год для assets)
- 🌐 Node.js версия 20

## 🎨 Что деплоится

При деплое на Netlify автоматически:
- ✅ Собирается React приложение
- ✅ Оптимизируются все assets
- ✅ Применяется Tailwind CSS
- ✅ Настраивается роутинг (TanStack Router)
- ✅ Добавляются security headers
- ✅ Настраивается кеширование

## 🛠️ Локальное тестирование продакшн сборки

Перед деплоем можно протестировать production сборку локально:

```bash
# Сборка проекта
npm run build

# Запуск локального preview сервера
npm run serve
```

Откройте [http://localhost:4173](http://localhost:4173) для просмотра.

## 🔍 Мониторинг деплоев

### Через GitHub Actions
1. Перейдите в **Actions** в вашем GitHub репозитории
2. Выберите workflow **"Deploy to Netlify"**
3. Смотрите статус и логи каждого деплоя

### Через Netlify Dashboard
1. Откройте [app.netlify.com](https://app.netlify.com)
2. Выберите ваш сайт
3. Перейдите в **Deploys** для просмотра истории

## ❗ Troubleshooting

### Деплой не запускается
- ✅ Проверьте, что секреты добавлены в GitHub (NETLIFY_AUTH_TOKEN, NETLIFY_SITE_ID)
- ✅ Проверьте имя ветки в `.github/workflows/netlify-deploy.yml` (должно совпадать с вашей основной веткой)

### Build failed
- ✅ Проверьте локально: `npm run check:safe`
- ✅ Посмотрите логи в GitHub Actions
- ✅ Убедитесь, что все зависимости установлены корректно

### Сайт не работает после деплоя
- ✅ Проверьте `netlify.toml` - должен быть редирект для SPA
- ✅ Убедитесь, что `publish = "dist"` указывает на правильную папку
- ✅ Проверьте логи деплоя в Netlify Dashboard

## 🌐 Custom Domain

Для добавления собственного домена:

1. Откройте сайт на Netlify
2. Перейдите в **Domain settings**
3. Нажмите **"Add custom domain"**
4. Следуйте инструкциям для настройки DNS

Netlify автоматически настроит:
- 🔒 HTTPS сертификат (Let's Encrypt)
- 🌍 CDN для быстрой загрузки по всему миру

## 📚 Дополнительные ресурсы

- 📖 [Netlify Documentation](https://docs.netlify.com/)
- 🔧 [GitHub Actions Documentation](https://docs.github.com/en/actions)
- ⚡ [Vite Documentation](https://vitejs.dev/)
- ⚛️ [React Documentation](https://react.dev/)

---

**🎉 Готово! Теперь каждый push в main/master будет автоматически деплоить ваш сайт на Netlify!**
