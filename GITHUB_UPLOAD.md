# 🚀 Инструкция по загрузке кода на GitHub

## ✅ Что уже сделано

- Git репозиторий инициализирован
- Весь код закоммичен в commit `18e10f4`
- Ветка переименована в `main`

## 📝 Следующие шаги

### Шаг 1: Создайте новый репозиторий на GitHub

1. Откройте https://github.com/new в браузере
2. Заполните:
   - **Repository name**: `vite-react-template` (или любое имя)
   - **Description**: `React 19 + TypeScript template with shadcn/ui`
   - **Visibility**: Public или Private (на ваш выбор)
3. **ВАЖНО**: НЕ добавляйте README, .gitignore или лицензию (у нас уже есть)
4. Нажмите **Create repository**

### Шаг 2: Свяжите локальный репозиторий с GitHub

После создания репозитория GitHub покажет инструкции. Используйте команды из раздела **"...or push an existing repository from the command line"**:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_REPO.git
git push -u origin main
```

**Замените** `ВАШ_USERNAME` и `ВАШ_REPO` на ваши данные!

#### Пример:
```bash
# Если ваш username: johndoe и название репо: my-app
git remote add origin https://github.com/johndoe/my-app.git
git push -u origin main
```

### Шаг 3: Проверьте результат

После выполнения команд:
1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все файлы проекта
3. Commit message будет: "Initial commit: React 19 + TypeScript template with shadcn/ui"

## 🎯 После загрузки

После успешной загрузки кода на GitHub, вы сможете:

1. **Настроить автоматический деплой на Netlify** - следуйте инструкциям в файле `АВТОДЕПЛОЙ.md`
2. **Пригласить соавторов** через настройки репозитория
3. **Создавать Pull Requests** для code review
4. **Использовать GitHub Actions** для CI/CD (уже настроено в `.github/workflows/netlify-deploy.yml`)

## ❓ Возможные проблемы

### Ошибка аутентификации

Если при `git push` появляется ошибка аутентификации:

**Вариант 1: HTTPS с Personal Access Token (рекомендуется)**
1. Создайте Personal Access Token: https://github.com/settings/tokens
2. Используйте его вместо пароля при push

**Вариант 2: SSH**
```bash
# Используйте SSH URL вместо HTTPS
git remote set-url origin git@github.com:ВАШ_USERNAME/ВАШ_REPO.git
git push -u origin main
```

### Репозиторий уже существует

Если удаленный репозиторий уже добавлен:
```bash
# Проверьте существующие remote
git remote -v

# Удалите старый origin
git remote remove origin

# Добавьте новый
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_REPO.git
```

## 📋 Быстрая проверка

Перед выполнением команд убедитесь:
```bash
# Проверьте статус git
git status
# Должно быть: "On branch main, nothing to commit, working tree clean"

# Проверьте последний коммит
git log -1 --oneline
# Должно быть: "18e10f4 Initial commit: React 19 + TypeScript template with shadcn/ui"

# Проверьте текущие remotes
git remote -v
# Должно быть пусто (если это первая загрузка)
```

## 🎉 Готово!

После успешной загрузки ваш код будет доступен на GitHub, и вы сможете:
- Делиться проектом с другими
- Настроить автоматический деплой
- Использовать GitHub Actions для CI/CD
- Управлять версиями и ветками

---

**Следующий шаг**: Откройте `АВТОДЕПЛОЙ.md` для настройки автоматического деплоя на Netlify!
