# 🚀 Быстрый старт: Синхронизация с Android Studio

## ⚡ За 30 секунд

```bash
# 1. Проверка статуса
npm run sync:status

# 2. Первая настройка (автоматически найдёт Android Studio проект)
npm run sync

# 3. Запуск автоматического мониторинга
npm run sync:watch &

# ✅ Готово! Теперь все изменения синхронизируются автоматически
```

---

## 📊 Что происходит автоматически?

После запуска `npm run sync:watch`:

1. **Каждые 5 секунд** система проверяет изменения
2. **При обнаружении изменений** автоматически запускает:
   - 🔄 Android Studio синхронизация (Web ⇄ AS)
   - 📱 Capacitor sync
   - 💾 Git commit + push в GitHub

---

## 🎯 Основные команды

```bash
# Проверка статуса всех систем
npm run sync:status

# Разовая синхронизация
npm run sync

# Автоматический мониторинг (рекомендуется)
npm run sync:watch

# Остановка мониторинга
pkill -f unified-sync

# Просмотр логов в реальном времени
tail -f unified-sync.log
```

---

## 🛠️ Ручная настройка (если автопоиск не сработал)

Если система не нашла Android Studio проект автоматически:

```bash
# 1. Создайте конфигурацию
cat > .android-studio-sync.conf << EOF
ANDROID_STUDIO_PROJECT="/путь/к/вашему/AndroidStudioProject"
SYNC_INTERVAL=5
AUTO_CAPACITOR_SYNC=true
WATCH_ANDROID_CHANGES=true
EXCLUDE_PATTERNS=".git|node_modules|dist|build|.gradle|.idea"
EOF

# 2. Проверьте
npm run sync:status

# 3. Запустите
npm run sync
```

---

## 🔧 Типичные проблемы и решения

### ❌ "Android Studio проект не найден"

```bash
# Отредактируйте путь в конфигурации
nano .android-studio-sync.conf
# Измените ANDROID_STUDIO_PROJECT="/правильный/путь"
```

### ❌ "Git remote не настроен"

```bash
# Добавьте remote
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main
```

### ❌ "Permission denied"

```bash
chmod +x unified-sync.sh
chmod +x android-studio-sync.sh
```

### ⚠️ "Could not find web assets directory"

Это нормально! Просто сначала сбилдите проект:

```bash
npm run build
```

---

## 📚 Полная документация

Для детальной информации смотрите: **[ANDROID_STUDIO_SYNC_README.md](ANDROID_STUDIO_SYNC_README.md)**

Включает:
- ✅ Все возможности системы
- ✅ Режимы работы
- ✅ Расширенная настройка
- ✅ Устранение всех проблем
- ✅ Лучшие практики
- ✅ FAQ

---

## ✨ Преимущества

- ✅ **Автоматизация** - забудьте о ручной синхронизации
- ✅ **Двунаправленная** - изменения в обе стороны
- ✅ **Умная** - игнорирует служебные файлы
- ✅ **Безопасная** - защита от параллельных процессов
- ✅ **Прозрачная** - полное логирование

---

## 🎉 Начните сейчас!

```bash
npm run sync:watch &
```

**Счастливого кодирования!** 🚀
