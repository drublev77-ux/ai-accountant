# 🚀 Автоматический деплой - Полная инструкция

Ваш проект настроен для автоматического деплоя на Netlify! Следуйте этой инструкции для запуска.

## ✅ Что уже готово

1. **Git репозиторий инициализирован** - проект под version control
2. **Код закоммичен** - все файлы сохранены в commit `bd8a76c`
3. **GitHub Actions workflow настроен** - `.github/workflows/netlify-deploy.yml`
4. **Netlify конфигурация готова** - `netlify.toml` с оптимальными настройками
5. **Проект собран и проверен** - `npm run build` выполнен успешно

## 📋 Что нужно сделать для деплоя

### Шаг 1: Загрузить код на GitHub

Поскольку это локальный репозиторий, вам нужно создать GitHub репозиторий и загрузить код:

```bash
# 1. Создайте новый репозиторий на GitHub (https://github.com/new)
#    Не добавляйте README, .gitignore или лицензию

# 2. Добавьте remote репозиторий (замените YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 3. Переименуйте ветку в main (если нужно)
git branch -M main

# 4. Загрузите код на GitHub
git push -u origin main
```

### Шаг 2: Создать сайт на Netlify

1. Откройте [app.netlify.com](https://app.netlify.com)
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **GitHub** и авторизуйтесь
4. Выберите ваш репозиторий
5. Netlify автоматически определит настройки:
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Node version: `20`
6. Нажмите **"Deploy site"**

### Шаг 3: Получить токены для GitHub Actions

#### 3.1 Получить NETLIFY_AUTH_TOKEN

1. Перейдите на [app.netlify.com/user/applications](https://app.netlify.com/user/applications)
2. Нажмите **"New access token"**
3. Введите описание: "GitHub Actions Deploy"
4. **СКОПИРУЙТЕ ТОКЕН** (он больше не будет показан!)

#### 3.2 Получить NETLIFY_SITE_ID

1. Откройте ваш сайт на Netlify
2. Перейдите в **Site settings** → **General**
3. В разделе **Site details** найдите **Site ID**
4. Скопируйте ID (формат: `1234abcd-5678-90ef-ghij-klmnopqrstuv`)

### Шаг 4: Добавить секреты в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**

**Добавьте два секрета:**

**Секрет 1:**
- Name: `NETLIFY_AUTH_TOKEN`
- Value: (токен из шага 3.1)

**Секрет 2:**
- Name: `NETLIFY_SITE_ID`
- Value: (ID из шага 3.2)

### Шаг 5: Запустить автодеплой

Теперь всё готово! Автоматический деплой запустится при:

```bash
# Любой push в ветку main
git push origin main

# Или создание Pull Request
```

## 🎯 Как это работает

### При каждом push в main:

1. GitHub Actions автоматически запускается
2. Устанавливаются зависимости (`npm ci`)
3. Выполняется проверка типов и линтинг (`npm run check:safe`)
4. Собирается production версия (`npm run build`)
5. Деплоится на Netlify
6. Netlify возвращает URL сайта

### При создании Pull Request:

1. Создаётся **preview deployment** с уникальным URL
2. GitHub добавляет комментарий с ссылкой на preview
3. После merge в main - деплоится в production

## 🔍 Мониторинг деплоев

### GitHub Actions
- Откройте **Actions** в вашем репозитории
- Выберите workflow "Deploy to Netlify"
- Смотрите статус и логи деплоя

### Netlify Dashboard
- Откройте [app.netlify.com](https://app.netlify.com)
- Выберите ваш сайт
- Вкладка **Deploys** - история всех деплоев

## 🎨 Что деплоится

При деплое автоматически:
- ✅ Собирается React приложение (React 19)
- ✅ Компилируется TypeScript в JavaScript
- ✅ Применяется Tailwind CSS
- ✅ Минифицируются и оптимизируются все файлы
- ✅ Настраивается роутинг (TanStack Router)
- ✅ Добавляются security headers (защита от XSS, clickjacking)
- ✅ Настраивается кеширование (1 год для статических файлов)

## ⚡ Быстрый старт (команды)

```bash
# 1. Создайте репозиторий на GitHub: https://github.com/new

# 2. Выполните эти команды (замените YOUR_USERNAME и YOUR_REPO):
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main

# 3. Настройте Netlify (см. Шаг 2-4 выше)

# 4. Готово! Теперь при каждом push:
git add .
git commit -m "Update: ваши изменения"
git push
```

## 🛠️ Локальное тестирование

Перед деплоем можно протестировать production сборку локально:

```bash
# Сборка проекта
npm run build

# Запуск локального preview сервера
npm run serve
```

Откройте http://localhost:4173

## 📊 Статус сборки

Текущий статус:
- ✅ Git репозиторий: Инициализирован
- ✅ Commit: `bd8a76c` "Add automatic deployment configuration"
- ✅ Build: Успешно (913.68 kB JS, 170.25 kB CSS)
- ⏳ GitHub: Нужно создать репозиторий и загрузить код
- ⏳ Netlify: Нужно настроить сайт и токены

## 🌐 После деплоя

Ваш сайт будет доступен по адресу:
- Production: `https://your-site-name.netlify.app`
- Preview (PR): `https://deploy-preview-123--your-site-name.netlify.app`

### Добавление собственного домена (опционально)

1. В Netlify: **Domain settings** → **Add custom domain**
2. Следуйте инструкциям для настройки DNS
3. Netlify автоматически настроит HTTPS (Let's Encrypt)

## ❗ Решение проблем

### Деплой не запускается
```bash
# Проверьте, что секреты добавлены в GitHub
# Settings → Secrets and variables → Actions
# Должны быть: NETLIFY_AUTH_TOKEN, NETLIFY_SITE_ID
```

### Build failed
```bash
# Проверьте локально
npm run check:safe

# Смотрите логи в GitHub Actions
# Actions → Deploy to Netlify → Latest run
```

### Сайт не работает после деплоя
```bash
# Проверьте, что в netlify.toml есть редирект для SPA
# (уже настроено в вашем проекте)

# Проверьте логи деплоя в Netlify Dashboard
# Site → Deploys → Latest deploy → Deploy log
```

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- ✅ Автоматический деплой при каждом push в main
- ✅ Preview деплой для каждого Pull Request
- ✅ HTTPS сертификат от Let's Encrypt
- ✅ CDN для быстрой загрузки по всему миру
- ✅ Оптимизированная production сборка

---

**Следующий шаг:** Создайте репозиторий на GitHub и выполните команды из раздела "Быстрый старт"!
