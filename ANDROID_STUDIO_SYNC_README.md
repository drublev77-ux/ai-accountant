# 🔄 Автоматическая синхронизация с Android Studio

Полное руководство по настройке и использованию автоматической двунаправленной синхронизации между вашим веб-проектом и Android Studio.

---

## 📋 Содержание

1. [Возможности](#возможности)
2. [Быстрый старт](#быстрый-старт)
3. [Команды](#команды)
4. [Настройка](#настройка)
5. [Режимы работы](#режимы-работы)
6. [Устранение неполадок](#устранение-неполадок)

---

## ✨ Возможности

### 🎯 Unified Sync - Единая система синхронизации

- ✅ **Git синхронизация** - автоматические коммиты и push в GitHub
- ✅ **Android Studio синхронизация** - двунаправленная синхронизация кода
- ✅ **Capacitor синхронизация** - автоматический `npx cap sync`
- ✅ **Автоматическое обнаружение** - находит Android Studio проект автоматически
- ✅ **Мониторинг изменений** - отслеживает изменения в обе стороны
- ✅ **Защита от дублирования** - умная блокировка параллельных процессов

### 🔍 Android Studio Sync - Специализированная синхронизация

- ✅ **Автопоиск проекта** - ищет в стандартных директориях
- ✅ **Двунаправленная синхронизация** - Web ⇄ Android Studio
- ✅ **Фильтрация файлов** - исключает build, .gradle, node_modules
- ✅ **Watch режим** - непрерывный мониторинг
- ✅ **Хеширование** - определяет реальные изменения

---

## 🚀 Быстрый старт

### 1. Проверка статуса

```bash
npm run sync:status
```

Покажет текущее состояние всех систем синхронизации.

### 2. Первая настройка

```bash
# Автоматическая настройка и разовая синхронизация
npm run sync
```

При первом запуске:
- Автоматически найдёт Android Studio проект
- Создаст конфигурационный файл `.android-studio-sync.conf`
- Выполнит начальную синхронизацию

### 3. Запуск автоматического мониторинга

```bash
# Запустить в фоновом режиме
npm run sync:watch &

# Или напрямую
./unified-sync.sh watch
```

Теперь система будет:
- Отслеживать изменения каждые 5 секунд
- Автоматически синхронизировать Git, Android Studio и Capacitor
- Записывать логи в `unified-sync.log`

---

## 📝 Команды

### NPM Scripts

```bash
# Unified Sync (рекомендуется)
npm run sync              # Разовая полная синхронизация
npm run sync:watch        # Автоматический мониторинг
npm run sync:status       # Проверка статуса

# Отдельные компоненты
npm run sync:git          # Только Git
npm run sync:capacitor    # Только Capacitor
npm run sync:android-studio  # Только Android Studio

# Android Studio Sync (специализированная)
npm run sync:as           # Разовая AS синхронизация
npm run sync:as:watch     # Мониторинг только AS
```

### Прямые команды

```bash
# Unified Sync
./unified-sync.sh watch          # Автомониторинг
./unified-sync.sh sync           # Разовая синхронизация
./unified-sync.sh status         # Статус
./unified-sync.sh help           # Справка

# Android Studio Sync
./android-studio-sync.sh watch   # Автомониторинг AS
./android-studio-sync.sh sync    # Разовая AS синхронизация
./android-studio-sync.sh to-as   # Только Web → AS
./android-studio-sync.sh from-as # Только AS → Web
```

---

## ⚙️ Настройка

### Автоматическая настройка (рекомендуется)

```bash
npm run sync
```

Система автоматически:
1. Найдёт Android Studio проект в стандартных местах:
   - `~/AndroidStudioProjects`
   - `~/StudioProjects`
   - `~/Projects`
   - `~/Documents/AndroidStudioProjects`
   - `~/Desktop`
2. Создаст конфигурацию `.android-studio-sync.conf`

### Ручная настройка

Если автопоиск не сработал, отредактируйте `.android-studio-sync.conf`:

```bash
# Автоматически создано 2025-12-12 10:30:00
ANDROID_STUDIO_PROJECT="/path/to/your/AndroidStudioProject"
SYNC_INTERVAL=5
AUTO_CAPACITOR_SYNC=true
WATCH_ANDROID_CHANGES=true
EXCLUDE_PATTERNS=".git|node_modules|dist|build|.gradle|.idea"
```

**Параметры:**
- `ANDROID_STUDIO_PROJECT` - путь к проекту Android Studio
- `SYNC_INTERVAL` - интервал проверки изменений (секунды)
- `AUTO_CAPACITOR_SYNC` - автоматический Capacitor sync
- `WATCH_ANDROID_CHANGES` - отслеживать изменения в AS
- `EXCLUDE_PATTERNS` - исключаемые директории

### Настройка Git Remote

```bash
# Создайте репозиторий на GitHub
# Затем добавьте remote:
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main
```

---

## 🎮 Режимы работы

### 1. Unified Sync (Объединённая синхронизация)

**Рекомендуется для продакшена**

```bash
npm run sync:watch
```

**Что делает:**
1. Мониторит изменения в `src/`, `android/`, `public/`
2. При обнаружении изменений запускает:
   - Android Studio синхронизация (двунаправленная)
   - Capacitor sync
   - Git commit + push

**Преимущества:**
- ✅ Полная автоматизация
- ✅ Все системы синхронизированы
- ✅ Единый лог файл
- ✅ Защита от дублирования

### 2. Android Studio Sync (Специализированная)

**Для работы только с Android Studio**

```bash
npm run sync:as:watch
```

**Что делает:**
- Мониторит изменения в обе стороны
- Синхронизирует только android/ директорию
- Запускает Capacitor sync при необходимости

**Когда использовать:**
- Активная разработка Android компонентов
- Нужна быстрая синхронизация без Git

### 3. Ручная синхронизация

```bash
# Полная синхронизация один раз
npm run sync

# Только Android Studio
npm run sync:android-studio

# Только Git
npm run sync:git

# Только Capacitor
npm run sync:capacitor
```

---

## 🔧 Типичные сценарии использования

### Сценарий 1: Начало нового дня разработки

```bash
# 1. Проверьте статус
npm run sync:status

# 2. Запустите автомониторинг
npm run sync:watch &

# 3. Откройте проект в Android Studio
# Теперь все изменения синхронизируются автоматически!
```

### Сценарий 2: Работа с конкретным компонентом Android

```bash
# 1. Синхронизируйте текущее состояние
npm run sync

# 2. Внесите изменения в Android Studio

# 3. Синхронизируйте обратно
npm run sync:as from-as
```

### Сценарий 3: Подготовка к деплою

```bash
# 1. Остановите автомониторинг (если запущен)
pkill -f unified-sync

# 2. Финальная синхронизация
npm run sync

# 3. Проверьте статус
npm run sync:status

# 4. Билд проекта
npm run build
npx cap sync
```

---

## 📊 Мониторинг и логи

### Проверка статуса

```bash
npm run sync:status
```

Показывает:
- ✅ Статус Git (remote, ветка, незакоммиченные изменения)
- ✅ Статус Android Studio (путь к проекту, доступность)
- ✅ Статус Capacitor (App ID, конфигурация)
- ✅ Активные процессы мониторинга
- ✅ Последние 5 событий из лога

### Просмотр логов

```bash
# Unified Sync логи
tail -f unified-sync.log

# Android Studio Sync логи (если использовался отдельно)
cat .android-studio-sync.conf
```

### Проверка процессов

```bash
# Найти запущенные процессы мониторинга
ps aux | grep -E "unified-sync|android-studio-sync" | grep -v grep

# Остановить все процессы мониторинга
pkill -f unified-sync
pkill -f android-studio-sync
```

---

## 🛠️ Устранение неполадок

### Проблема: Android Studio проект не найден автоматически

**Решение:**

```bash
# 1. Создайте конфигурацию вручную
cat > .android-studio-sync.conf << EOF
ANDROID_STUDIO_PROJECT="/полный/путь/к/вашему/проекту"
SYNC_INTERVAL=5
AUTO_CAPACITOR_SYNC=true
WATCH_ANDROID_CHANGES=true
EXCLUDE_PATTERNS=".git|node_modules|dist|build|.gradle|.idea"
EOF

# 2. Проверьте
npm run sync:status
```

### Проблема: Git push fails

**Решение:**

```bash
# 1. Проверьте remote
git remote -v

# 2. Если нет remote, добавьте
git remote add origin https://github.com/username/repo.git

# 3. Если есть проблемы с авторизацией, используйте SSH или токен
git remote set-url origin git@github.com:username/repo.git
```

### Проблема: Permission denied на скриптах

**Решение:**

```bash
chmod +x unified-sync.sh
chmod +x android-studio-sync.sh
```

### Проблема: Capacitor sync warning

**Это нормально!** Если вы видите:

```
⚠️  Could not find the web assets directory: ./dist
```

Это означает что нужно сначала сбилдить проект:

```bash
npm run build
```

### Проблема: Процесс мониторинга "зависает"

**Решение:**

```bash
# 1. Остановите все процессы
pkill -f unified-sync
pkill -f android-studio-sync

# 2. Удалите lock файлы
rm -f /tmp/android-studio-sync.lock

# 3. Перезапустите
npm run sync:watch
```

### Проблема: Слишком частые синхронизации

**Решение:** Увеличьте интервал в `.android-studio-sync.conf`:

```bash
SYNC_INTERVAL=10  # Было 5
```

---

## 🎯 Лучшие практики

### 1. Используйте Unified Sync для продакшена

```bash
npm run sync:watch &
```

Это обеспечивает полную автоматизацию всех систем.

### 2. Регулярно проверяйте статус

```bash
npm run sync:status
```

Особенно перед важными коммитами или деплоем.

### 3. Следите за логами

```bash
tail -f unified-sync.log
```

Это поможет быстро заметить проблемы.

### 4. Делайте резервные копии конфигурации

```bash
cp .android-studio-sync.conf .android-studio-sync.conf.backup
```

### 5. Используйте .gitignore

Убедитесь что следующие файлы в `.gitignore`:

```
.android-studio-sync.conf
unified-sync.log
/tmp/android-studio-sync.lock
```

---

## 📚 Дополнительные ресурсы

### Связанные файлы

- `unified-sync.sh` - Главный скрипт объединённой синхронизации
- `android-studio-sync.sh` - Специализированная AS синхронизация
- `.android-studio-sync.conf` - Конфигурация (создаётся автоматически)
- `unified-sync.log` - Лог файл

### Документация

- [Capacitor Documentation](https://capacitorjs.com/docs)
- [Git Documentation](https://git-scm.com/doc)
- [Android Studio](https://developer.android.com/studio)

---

## ❓ FAQ

**Q: Как часто происходит синхронизация?**
A: По умолчанию каждые 5 секунд. Настраивается в `.android-studio-sync.conf`.

**Q: Можно ли синхронизировать только в одну сторону?**
A: Да, используйте:
```bash
./android-studio-sync.sh to-as   # Web → Android Studio
./android-studio-sync.sh from-as # Android Studio → Web
```

**Q: Что происходит при конфликтах?**
A: Система использует rsync который сохраняет новейшие изменения. При серьёзных конфликтах используйте Git.

**Q: Безопасно ли запускать несколько процессов?**
A: Нет, система использует lock файлы для предотвращения параллельных запусков.

**Q: Работает ли это на Windows?**
A: Скрипты написаны для Linux/macOS. На Windows используйте WSL или Git Bash с установленным rsync.

---

## 🎉 Готово!

Теперь у вас настроена полная автоматическая синхронизация между веб-проектом и Android Studio!

**Рекомендуемая команда для старта:**

```bash
npm run sync:watch &
```

Счастливого кодирования! 🚀
