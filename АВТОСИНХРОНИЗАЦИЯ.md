# 🔄 Автоматическая синхронизация проекта

Система автоматической синхронизации для разработки React приложения с автоматической синхронизацией с Android Studio.

---

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Доступные методы синхронизации](#доступные-методы-синхронизации)
3. [Настройка Git синхронизации](#настройка-git-синхронизации)
4. [Настройка синхронизации с Android Studio](#настройка-синхронизации-с-android-studio)
5. [Автоматическая синхронизация при изменениях](#автоматическая-синхронизация-при-изменениях)
6. [Команды npm](#команды-npm)
7. [Решение проблем](#решение-проблем)

---

## 🚀 Быстрый старт

### Вариант 1: Git синхронизация (рекомендуется)

```bash
# 1. Настроить удаленный репозиторий
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git

# 2. Запустить синхронизацию
npm run sync
```

### Вариант 2: Локальная синхронизация с Android Studio

```bash
# 1. Настроить конфигурацию
npm run sync:studio  # Создаст .sync-config.local

# 2. Отредактировать .sync-config.local
# 3. Запустить синхронизацию
npm run sync:studio
```

### Вариант 3: Автоматическая синхронизация

```bash
# Запустить мониторинг изменений (работает постоянно)
npm run sync:watch
```

---

## 🔧 Доступные методы синхронизации

### 1️⃣ Git синхронизация
**Описание**: Автоматический коммит и push изменений в Git репозиторий  
**Скрипт**: `auto-sync.sh`  
**Команда**: `npm run sync`

**Преимущества**:
- ✅ Работает везде (E2B, локально, CI/CD)
- ✅ История изменений
- ✅ Легко настроить

**Недостатки**:
- ❌ Требует GitHub/GitLab аккаунт
- ❌ Нужен интернет

---

### 2️⃣ Синхронизация с Android Studio (rsync/SSH)
**Описание**: Прямая синхронизация файлов с локальным компьютером  
**Скрипт**: `sync-android-studio.sh`  
**Команда**: `npm run sync:studio`

**Преимущества**:
- ✅ Прямая синхронизация без Git
- ✅ Быстрая передача файлов
- ✅ Автоматический запуск `cap sync`

**Недостатки**:
- ❌ Требует настройку SSH или локальный доступ
- ❌ Сложнее настроить

---

### 3️⃣ Watch режим (автоматическая синхронизация)
**Описание**: Мониторинг изменений и автоматическая синхронизация  
**Скрипт**: `watch-sync.sh`  
**Команда**: `npm run sync:watch`

**Преимущества**:
- ✅ Полностью автоматический
- ✅ Синхронизация в реальном времени
- ✅ Debounce для предотвращения частых синхронизаций

**Недостатки**:
- ❌ Требует inotifywait (Linux) или fswatch (macOS)

---

## 🔐 Настройка Git синхронизации

### Шаг 1: Создать GitHub репозиторий

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например, `ai-accountant-app`)
3. **НЕ** инициализируйте с README/License

### Шаг 2: Добавить remote в проект

```bash
# Добавить удаленный репозиторий
git remote add origin https://github.com/ваш-username/ai-accountant-app.git

# Или через SSH (если настроен)
git remote add origin git@github.com:ваш-username/ai-accountant-app.git
```

### Шаг 3: Первая синхронизация

```bash
# Запустить синхронизацию
npm run sync

# Или с Capacitor sync
npm run sync:cap
```

### Результат:
- ✅ Все изменения закоммичены
- ✅ Изменения отправлены на GitHub
- ✅ Можно клонировать на любом компьютере

---

## 🏗️ Настройка синхронизации с Android Studio

### Вариант A: Локальная синхронизация (на одном компьютере)

```bash
# 1. Создать конфигурацию
npm run sync:studio
```

**Отредактируйте `.sync-config.local`:**

```bash
# Метод синхронизации
SYNC_METHOD="rsync"

# Путь к Android Studio проекту на ЭТОМ компьютере
LOCAL_ANDROID_PATH="/Users/username/AndroidStudioProjects/AIAccountant"

# Остальное оставьте пустым
REMOTE_ANDROID_PATH=""
GIT_REMOTE_URL=""

# Автоматический cap sync после копирования
AUTO_CAP_SYNC=true
```

```bash
# 2. Запустить синхронизацию
npm run sync:studio
```

---

### Вариант B: Удаленная синхронизация (через SSH)

**Требования**: SSH доступ к компьютеру с Android Studio

```bash
# 1. Настроить SSH ключи (на вашем компьютере)
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
ssh-copy-id username@192.168.1.100
```

**Отредактируйте `.sync-config.local`:**

```bash
SYNC_METHOD="rsync"

# Формат: username@IP:/полный/путь
REMOTE_ANDROID_PATH="username@192.168.1.100:/Users/username/AndroidStudioProjects/AIAccountant"

# Остальное оставьте пустым
LOCAL_ANDROID_PATH=""
GIT_REMOTE_URL=""

AUTO_CAP_SYNC=true
```

```bash
# 2. Запустить синхронизацию
npm run sync:studio
```

---

## ⚡ Автоматическая синхронизация при изменениях

### Linux (Ubuntu/Debian)

```bash
# 1. Установить inotify-tools
sudo apt-get install inotify-tools

# 2. Запустить watch режим
npm run sync:watch
```

### macOS

```bash
# 1. Установить fswatch
brew install fswatch

# 2. Запустить watch режим
npm run sync:watch
```

### Что происходит:
- 🔍 Мониторинг директорий: `src`, `android/app/src`, `ios/App`, `public`
- ⏱️ Debounce: 3 секунды (не синхронизирует слишком часто)
- 📝 Логирование: все действия пишутся в `sync.log`
- 🔄 Автоматический `cap sync` после изменений

---

## 📦 Команды npm

| Команда | Описание |
|---------|----------|
| `npm run sync` | Однократная Git синхронизация |
| `npm run sync:cap` | Git синхронизация + Capacitor sync |
| `npm run sync:studio` | Синхронизация с Android Studio |
| `npm run sync:watch` | Автоматическая синхронизация при изменениях |

---

## 🛠️ Решение проблем

### Проблема: "Permission denied" при rsync

**Решение**:
```bash
# Проверить SSH подключение
ssh username@192.168.1.100

# Проверить права доступа к директории
ls -la /Users/username/AndroidStudioProjects/
```

---

### Проблема: "git push rejected"

**Решение**:
```bash
# Получить изменения с сервера
git pull --rebase origin main

# Затем снова запустить синхронизацию
npm run sync
```

---

### Проблема: "inotifywait not found"

**Решение**:
```bash
# Ubuntu/Debian
sudo apt-get install inotify-tools

# macOS
brew install fswatch
```

---

### Проблема: Watch режим не работает

**Решение 1**: Использовать polling режим (автоматически включается)

**Решение 2**: Запускать синхронизацию вручную
```bash
# После каждого изменения
npm run sync
```

---

## 🔄 Рабочий процесс (Workflow)

### Вариант 1: GitHub + Android Studio

```bash
# Terminal 1 (E2B): Запустить watch режим
npm run sync:watch

# Теперь просто редактируйте код в E2B
# Изменения автоматически пушатся в GitHub

# На локальном компьютере:
git pull  # Получить изменения
# Открыть в Android Studio
```

---

### Вариант 2: Прямая синхронизация rsync

```bash
# Terminal 1 (E2B): Запустить watch режим
npm run sync:watch

# Изменения автоматически копируются на локальный компьютер
# Android Studio автоматически обнаружит изменения
```

---

### Вариант 3: Ручная синхронизация

```bash
# После каждого изменения
npm run sync:studio
```

---

## 📝 Логирование

Все действия синхронизации записываются в файл `sync.log`:

```bash
# Просмотр логов в реальном времени
tail -f sync.log

# Просмотр последних 50 строк
tail -n 50 sync.log

# Очистка логов
> sync.log
```

---

## 🎯 Рекомендации

### Для простоты: Git синхронизация
```bash
git remote add origin https://github.com/username/repo.git
npm run sync:watch
```

### Для скорости: rsync синхронизация
```bash
# Настроить .sync-config.local
npm run sync:watch
```

### Для надежности: Оба метода
```bash
# Git для истории + rsync для быстрой разработки
```

---

## ✅ Готово!

Теперь ваш проект автоматически синхронизируется! 🎉

**Следующие шаги**:
1. ✅ Выбрать метод синхронизации
2. ✅ Настроить конфигурацию
3. ✅ Запустить `npm run sync:watch`
4. ✅ Начать разработку!
