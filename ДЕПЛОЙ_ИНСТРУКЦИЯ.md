# 🚀 Полная инструкция по деплою AI Accountant

## ✅ Статус готовности

Ваш проект **полностью готов** к деплою на все три платформы:

- ✅ GitHub Pages - автоматический деплой настроен
- ✅ Vercel - конфигурация готова
- ✅ Netlify - конфигурация готова

---

## 🎯 Деплой на все платформы (пошаговая инструкция)

### Предварительные требования

1. **Git репозиторий** уже существует: `https://github.com/drublev77-ux/ai-accountant`
2. **Все конфигурационные файлы** уже созданы
3. **Проект собран** и протестирован

---

## 📋 План действий

### ✅ Шаг 1: GitHub Pages (АВТОМАТИЧЕСКИЙ ДЕПЛОЙ)

**Статус:** Уже настроено! Нужно только активировать.

#### Действия:

1. **Откройте настройки GitHub Pages:**
   ```
   https://github.com/drublev77-ux/ai-accountant/settings/pages
   ```

2. **В разделе "Source" выберите:**
   - Source: **GitHub Actions** (не "Deploy from a branch")

3. **Сохраните изменения**

4. **Загрузите код на GitHub:**
   ```bash
   cd /home/user/vite-template
   git add .
   git commit -m "Настройка деплоя на все платформы"
   git push origin main
   ```

5. **Проверьте статус деплоя:**
   ```
   https://github.com/drublev77-ux/ai-accountant/actions
   ```

#### Результат:

После успешного деплоя ваше приложение будет доступно:
```
https://drublev77-ux.github.io/ai-accountant/
```

**Автоматический деплой:** Каждый `git push` в `main` автоматически обновит сайт!

---

### ✅ Шаг 2: Vercel

#### Вариант A: Через веб-интерфейс (Рекомендуется)

1. **Зайдите на Vercel:**
   ```
   https://vercel.com
   ```

2. **Войдите через GitHub**

3. **Нажмите "Add New Project"**

4. **Импортируйте репозиторий:**
   - Выберите `drublev77-ux/ai-accountant`
   - Нажмите "Import"

5. **Настройки проекта:**
   - Framework Preset: **Vite**
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

6. **Нажмите "Deploy"**

#### Вариант B: Через CLI

```bash
# Установка Vercel CLI
npm install -g vercel

# Вход в аккаунт
vercel login

# Деплой
vercel --prod

# При первом запуске ответьте:
# - Set up and deploy? → Yes
# - Which scope? → ваш username
# - Link to existing project? → No
# - Project name? → ai-accountant
# - Directory? → ./
# - Override settings? → No
```

#### Настройка секретов (для CI/CD):

Если хотите деплоить через GitHub Actions:

1. **Получите токены:**
   ```bash
   vercel login
   vercel link  # Создаст .vercel/project.json
   cat .vercel/project.json
   ```

2. **Добавьте секреты в GitHub:**
   ```
   https://github.com/drublev77-ux/ai-accountant/settings/secrets/actions
   ```

   Нажмите "New repository secret" и добавьте:
   - `VERCEL_TOKEN` - получите на https://vercel.com/account/tokens
   - `VERCEL_ORG_ID` - из `.vercel/project.json`
   - `VERCEL_PROJECT_ID` - из `.vercel/project.json`

3. **Деплой через GitHub Actions** будет автоматическим!

#### Результат:

```
https://ai-accountant.vercel.app
```

---

### ✅ Шаг 3: Netlify

#### Вариант A: Через веб-интерфейс (Рекомендуется)

1. **Зайдите на Netlify:**
   ```
   https://netlify.com
   ```

2. **Войдите через GitHub**

3. **Нажмите "Add new site" → "Import an existing project"**

4. **Выберите источник:**
   - Нажмите "GitHub"
   - Выберите репозиторий `drublev77-ux/ai-accountant`

5. **Настройки сборки** (автоматически заполнятся из `netlify.toml`):
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Node version: `20`

6. **Нажмите "Deploy site"**

#### Вариант B: Через CLI

```bash
# Установка Netlify CLI
npm install -g netlify-cli

# Вход в аккаунт
netlify login

# Деплой
netlify deploy --prod

# При первом запуске ответьте:
# - Create & configure new site? → Yes
# - Team? → выберите свою команду
# - Site name? → ai-accountant (или пустое для авто-генерации)
# - Publish directory? → dist
```

#### Настройка секретов (для CI/CD):

Если хотите деплоить через GitHub Actions:

1. **Получите токены:**
   ```bash
   netlify login
   netlify sites:list
   # Скопируйте Site ID
   ```

2. **Получите Auth Token:**
   ```
   https://app.netlify.com/user/applications#personal-access-tokens
   ```
   Создайте новый токен.

3. **Добавьте секреты в GitHub:**
   ```
   https://github.com/drublev77-ux/ai-accountant/settings/secrets/actions
   ```

   Нажмите "New repository secret" и добавьте:
   - `NETLIFY_AUTH_TOKEN` - токен с предыдущего шага
   - `NETLIFY_SITE_ID` - Site ID из команды `netlify sites:list`

4. **Деплой через GitHub Actions** будет автоматическим!

#### Результат:

```
https://ai-accountant.netlify.app
```
(или ваш custom URL)

---

## 🎉 Итоговая сводка

После выполнения всех шагов у вас будет:

| Платформа     | URL                                           | Статус деплоя                                      |
|---------------|-----------------------------------------------|---------------------------------------------------|
| GitHub Pages  | https://drublev77-ux.github.io/ai-accountant/ | Автоматический (при push в main)                   |
| Vercel        | https://ai-accountant.vercel.app              | Автоматический (при push в main)                   |
| Netlify       | https://ai-accountant.netlify.app             | Автоматический (при push в main)                   |

---

## 🔄 Автоматический деплой

После настройки всех платформ:

```bash
# Внесите изменения в код
git add .
git commit -m "Обновление приложения"
git push origin main

# Все три платформы автоматически обновятся!
```

**Отслеживание деплоя:**

- **GitHub Pages:** https://github.com/drublev77-ux/ai-accountant/actions
- **Vercel:** https://vercel.com/dashboard
- **Netlify:** https://app.netlify.com/sites/ai-accountant/deploys

---

## 📝 Дополнительные настройки

### Custom Domain (опционально)

**Vercel:**
1. Project Settings → Domains
2. Add Domain → введите ваш домен

**Netlify:**
1. Site Settings → Domain Management
2. Add Custom Domain → введите ваш домен

**GitHub Pages:**
1. Settings → Pages → Custom domain
2. Введите ваш домен

### Environment Variables

Если нужны переменные окружения (API ключи):

**Vercel:**
```
Project Settings → Environment Variables
```

**Netlify:**
```
Site Settings → Environment Variables
```

**Пример переменных:**
```bash
VITE_STRIPE_PUBLIC_KEY=pk_live_xxxxx
VITE_PAYPAL_CLIENT_ID=xxxxx
```

---

## ✅ Чеклист готовности

- [ ] GitHub Pages активирован (Source: GitHub Actions)
- [ ] Код загружен на GitHub (`git push origin main`)
- [ ] Vercel проект создан и связан с репозиторием
- [ ] Netlify сайт создан и связан с репозиторием
- [ ] Все деплои успешно завершены
- [ ] Проверены все три URL

---

## 🆘 Помощь и поддержка

### GitHub Pages не работает?

**Проверьте:**
1. Settings → Pages → Source = **GitHub Actions**
2. Actions → Check workflow status
3. Проблема: "Get Pages site failed" → убедитесь, что `enablement: true` в `.github/workflows/github-pages.yml:43`

### Vercel не деплоится?

**Проверьте:**
1. Build command: `npm run build`
2. Output directory: `dist`
3. Node version: 22 или 20

### Netlify не деплоится?

**Проверьте:**
1. Publish directory: `dist`
2. Build command: `npm run build`
3. Node version в `netlify.toml:11`: `20`

### Общие проблемы:

```bash
# Проверка локальной сборки
npm run check:safe
npm run build

# Если сборка успешна локально, проблема в настройках платформы
```

---

## 📚 Документация

- **GitHub Pages:** [docs](https://docs.github.com/en/pages)
- **Vercel:** [docs](https://vercel.com/docs)
- **Netlify:** [docs](https://docs.netlify.com)

---

## 🎯 Быстрый старт (TL;DR)

### Минимальные действия для деплоя на все платформы:

```bash
# 1. GitHub Pages
# Откройте: https://github.com/drublev77-ux/ai-accountant/settings/pages
# Выберите: Source → GitHub Actions

# 2. Загрузите код
git add .
git commit -m "Деплой на все платформы"
git push origin main

# 3. Vercel (веб-интерфейс)
# https://vercel.com → Add New Project → Import ai-accountant

# 4. Netlify (веб-интерфейс)
# https://netlify.com → Add new site → Import ai-accountant

# Готово! 🎉
```

---

## 🚀 Статус: ГОТОВО К ЗАПУСКУ

Все файлы конфигурации созданы:
- ✅ `.github/workflows/github-pages.yml`
- ✅ `.github/workflows/deploy.yml` (Vercel)
- ✅ `.github/workflows/netlify-deploy.yml`
- ✅ `vercel.json`
- ✅ `netlify.toml`

**Следуйте инструкциям выше, и через 5 минут ваше приложение будет доступно на всех трёх платформах!** 🌍
