# Sentry CLI configuration
# Get your auth token from: https://sentry.io/settings/account/api/auth-tokens/

defaults.url=https://sentry.io/
defaults.org=den-49
defaults.project=vite-template

# Auth token (also can be set via SENTRY_AUTH_TOKEN environment variable)
# auth.token=YOUR_SENTRY_AUTH_TOKEN
